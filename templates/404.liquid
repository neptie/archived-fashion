<div class="container-fluid py-24 text-center">
  <div class="glass-panel max-w-2xl mx-auto p-12">
    <h1 class="text-6xl font-stencil mb-4 text-accent">404</h1>
    <p class="text-2xl font-stencil mb-8">Page Not Found</p>
    
    <p class="mb-8">The page you're looking for doesn't exist or has been moved.</p>
    
    <a href="/" class="arcade-button inline-block">
      Return Home
    </a>
  </div>
</div>

<style>
.glass-panel {
  position: relative;
  overflow: hidden;
}

.glass-panel::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--accent), transparent);
}

.glass-panel::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--accent), transparent);
}
</style> 