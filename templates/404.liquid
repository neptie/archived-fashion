<div class="error-page min-h-[70vh] flex items-center justify-center relative py-12">
  <div class="absolute inset-0 station-pattern"></div>
  
  <div class="glass-panel max-w-2xl mx-auto px-8 py-12 text-center relative">
    <div class="error-sign mb-8">
      <h1 class="text-6xl font-mono neon-glow mb-2">404</h1>
      <div class="platform-line"></div>
    </div>

    <div class="mb-8">
      <h2 class="text-2xl font-mono mb-2">
        <span class="english">Page Not Found</span>
        <span class="japanese">ページが見つかりません</span>
      </h2>
      <p class="text-gray-400 mb-4">
        <span class="english">The train has departed from this platform.</span>
        <span class="japanese">この路線は運休中です</span>
      </p>
    </div>

    <div class="space-y-4">
      <p class="text-sm text-gray-400">
        <span class="english">Please check the route map or return to the main station.</span>
        <span class="japanese">路線図をご確認いただくか、メイン駅にお戻りください。</span>
      </p>
      
      <a href="/" class="arcade-button inline-block">
        <span class="english">Return Home</span>
        <span class="japanese">ホームへ戻る</span>
      </a>
    </div>

    <!-- Decorative Elements -->
    <div class="absolute left-0 top-0 w-4 h-4 border-l-2 border-t-2 border-neon-pink"></div>
    <div class="absolute right-0 top-0 w-4 h-4 border-r-2 border-t-2 border-neon-pink"></div>
    <div class="absolute left-0 bottom-0 w-4 h-4 border-l-2 border-b-2 border-neon-pink"></div>
    <div class="absolute right-0 bottom-0 w-4 h-4 border-r-2 border-b-2 border-neon-pink"></div>
  </div>
</div>

<style>
.error-page {
  background: linear-gradient(to bottom, var(--bg-dark), var(--bg-darker));
}

.error-sign {
  position: relative;
}

.error-sign::before,
.error-sign::after {
  content: '404';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  color: var(--neon-pink);
  text-shadow: 0 0 10px var(--neon-pink);
}

.error-sign::before {
  animation: glitch-1 2s infinite;
  clip-path: polygon(0 0, 100% 0, 100% 45%, 0 45%);
  transform: translate(-2px);
}

.error-sign::after {
  animation: glitch-2 2s infinite;
  clip-path: polygon(0 60%, 100% 60%, 100% 100%, 0 100%);
  transform: translate(2px);
}

@keyframes glitch-1 {
  0%, 5%, 95%, 100% { transform: translate(0); }
  20%, 80% { transform: translate(-2px); }
  40%, 60% { transform: translate(2px); }
}

@keyframes glitch-2 {
  0%, 5%, 95%, 100% { transform: translate(0); }
  20%, 80% { transform: translate(2px); }
  40%, 60% { transform: translate(-2px); }
}

.arcade-button {
  position: relative;
  padding: 0.75rem 2rem;
  background: rgba(255, 16, 240, 0.1);
  border: 2px solid var(--neon-pink);
  border-radius: 4px;
  overflow: hidden;
  transition: all 0.3s ease;
}

.arcade-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 16, 240, 0.2),
    transparent
  );
  transition: 0.5s;
}

.arcade-button:hover::before {
  left: 100%;
}

.arcade-button:hover {
  box-shadow: 0 0 20px rgba(255, 16, 240, 0.4);
  transform: translateY(-2px);
}
</style> 